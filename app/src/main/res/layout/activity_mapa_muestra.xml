<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.MapaMuestraActivity">


    <fragment
        android:id="@+id/mapMuestra"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="10dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true">

        <Button
            android:id="@+id/btnCancelarMuestra"
            android:layout_width="115dp"
            android:layout_height="37dp"
            android:textAllCaps="false"
            android:text="Cancelar"
            android:textColor="@color/colorBlanco"
            android:background="@drawable/btn_cancelar_perzonalizado" />

        <Button
            android:id="@+id/btnGuardarMuestra"
            android:layout_width="115dp"
            android:layout_height="37dp"
            android:textAllCaps="false"
            android:text="Guardar"
            android:layout_marginStart="20dp"
            android:textColor="@color/colorBlanco"
            android:background="@drawable/btn_guardar_perzonalizado" />

    </LinearLayout>
</RelativeLayout>