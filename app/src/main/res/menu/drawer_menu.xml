<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <group
            android:id="@+id/nav_g1"
            android:checkableBehavior="single">
            <item
                android:id="@+id/nav_crecimiento"
                android:title="Monitoreo de crecimiento"
                android:icon="@drawable/ic_monitor"/>
            <item
                android:id="@+id/nav_muestras"
                android:title="Muestras"
                android:icon="@drawable/ic_muestras"/>
            <item
                android:id="@+id/nav_folio"
                android:title="Buscar por folio"
                android:icon="@drawable/ic_buscar_folio"/>

        </group>
        <group
            android:id="@+id/nav_g2"
            android:checkableBehavior="single">
            <item
                android:id="@+id/nav_sesion"
                android:title="Cerrar sesión"
                android:icon="@drawable/ic_log_out"/>

        </group>
    </group>
</menu>